Функционал Android-приложения (Kotlin + WebView) в проекте:

📱 Основная концепция:
Гибридное приложение на Kotlin + WebView, которое загружает три сайта экосистемы с нативной обёрткой.
Цель: быстрое развёртывание, кроссплатформенная логика на вебе, нативные функции через WebView API.

✅ Основные функции:
1. Вкладки (TabBar / Bottom Navigation):
Стартапы → загружает startuprossii.ru

Эксперты → загружает expertrossii.ru

Инвесторы → загружает investorrossii.ru

Личный кабинет → отдельный WebView с /my или нативный экран авторизации

2. Авторизация и сессия:
Сохранение JWT-токена в Android Keystore / Secure Storage

Автоматическая подстановка токена в заголовки запросов WebView

Возможность выхода из аккаунта с очисткой данных сессии

3. Push-уведомления через FCM (Firebase Cloud Messaging):
Регистрация устройства в FCM

Передача FCM-токена на бэкенд (/api/v1/users/me/fcm-token)

Получение пуша при:

Новой заявке на профиль

Изменении статуса модерации

Успешной оплате

Новом сообщении во внутреннем inbox

Нативное отображение уведомлений даже при закрытом приложении

4. Deep Links:
Поддержка ссылок вида:

app://startup/123 → открывает карточку стартапа

app://expert/456 → открывает карточку эксперта

app://investor/789 → открывает карточку инвестора

Интеграция с веб-сайтом: переходы из пушей или писем открывают нужный экран в приложении

5. Нативные элементы:
Splash Screen с логотипом экосистемы

Нативное меню для быстрого переключения между витринами

Кнопка «Назад» с обработкой истории WebView

Обновление (Pull-to-refresh) в каждом WebView

Индикатор загрузки (ProgressBar) при загрузке страниц

6. Интеграция с Android-системой:
Доступ к камере и галерее для загрузки аватаров и файлов (через WebView API)

Поддержка ночной темы (автоопределение системы)

Работа с сетевыми состояниями (офлайн-режим, уведомление об отсутствии интернета)

7. Локальное кэширование:
Кэширование статических ресурсов (CSS, JS, изображения) для ускорения загрузки

Очистка кэша в настройках приложения

⚙️ Технические особенности:
Архитектура:
Минималистичная Activity с WebView

Отдельный Service для работы с FCM

Менеджер сессии (SessionManager) для работы с токенами

Диспетчер deep links (DeepLinkHandler)

Безопасность:
JWT хранится в зашифрованном виде (Android Keystore)

WebView с включёнными настройками безопасности:

Отключение JavaScript-инъекций

Блокировка смешанного контента (HTTP в HTTPS)

Валидация доменов (разрешены только startuprossii.ru, expertrossii.ru, investorrossii.ru)

Конфигурация WebView:
kotlin
webView.settings.javaScriptEnabled = true
webView.settings.domStorageEnabled = true
webView.settings.allowFileAccess = false
webView.webViewClient = CustomWebViewClient()
webView.webChromeClient = CustomWebChromeClient() // для загрузки файлов
📲 Пользовательский сценарий:
Первый запуск:

Показ Splash Screen

Проверка наличия токена

Если нет → открывается страница авторизации в WebView

Работа с профилем:

Пользователь выбирает вкладку «Личный кабинет»

WebView загружает /my с подставленным токеном

Всё взаимодействие — через веб-интерфейс

Получение уведомления:

Приходит push о новой заявке

Пользователь тапает на уведомление

Приложение открывает deep link app://applications/123

WebView загружает страницу с деталями заявки

Создание заявки:

Пользователь просматривает карточку инвестора

Нажимает «Отправить заявку» (веб-кнопка)

WebView открывает форму

Данные отправляются через API

🛠️ Дополнительные возможности (опционально):
Биометрия / Face ID для быстрого входа

Виджет на главный экран с последними заявками

Интеграция с календарём для записи на консультации (эксперты)

Офлайн-режим для просмотра кэшированных карточек

📦 Что входит в сборку:
Исходный код на Kotlin

Конфигурация Firebase (FCM)

Настройки сборки (Gradle)

Иконки приложения (adaptive icons)

Инструкция по сборке и публикации в Google Play

Итог:
Приложение служит удобным мобильным клиентом для экосистемы, сочетая скорость веб-разработки с нативными возможностями Android (пуши, deep links, безопасное хранение). Оно полностью готово к использованию и публикации в Google Play.
